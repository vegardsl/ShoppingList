<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.stjerna.android.shoppinglist">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".usecase.login.LoginActivity">

        </activity>
        <activity android:name=".LauncherActivity">

        </activity>
        <activity android:name=".usecase.signup.SignUpActivity" />
        <activity android:name="com.stjerna.android.shoppinglist.usecase.createlist.DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.stjerna.android.shoppinglist.usecase.editlist.ManageListActivity" />
        <activity android:name="com.stjerna.android.shoppinglist.usecase.shopping.ShoppingActivity" />
    </application>
</manifest>
